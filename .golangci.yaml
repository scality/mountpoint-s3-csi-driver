version: "2"

run:
  timeout: 5m
  build-tags:
    - mage

linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
  exclusions:
    paths:
      - tests/controller
      - tests/sanity
