apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: upgrade-test-pvc
  namespace: default
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: ""
  resources:
    requests:
      storage: 10Gi
  volumeName: upgrade-test-pv
