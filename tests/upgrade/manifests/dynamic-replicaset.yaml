apiVersion: apps/v1
kind: ReplicaSet
metadata:
  name: upgrade-test-dynamic-replicaset
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: upgrade-test-dynamic
  template:
    metadata:
      labels:
        app: upgrade-test-dynamic
    spec:
      containers:
      - name: test
        image: busybox:latest
        command: ["sleep", "3600"]
        volumeMounts:
        - name: s3-volume
          mountPath: /data
      volumes:
      - name: s3-volume
        persistentVolumeClaim:
          claimName: upgrade-test-dynamic-pvc
