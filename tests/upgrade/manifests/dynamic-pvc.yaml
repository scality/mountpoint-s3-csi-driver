apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: upgrade-test-dynamic-pvc
  namespace: default
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: upgrade-test-sc
  resources:
    requests:
      storage: 10Gi
